apiVersion: v1
kind: Service
metadata:
  name: {{ template "singleservice.fullname" . }}
  labels:
    app: {{ template "singleservice.name" . }}
    chart: {{ template "singleservice.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "singleservice.name" . }}
    release: {{ .Release.Name }}
